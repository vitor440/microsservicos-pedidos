server:
  port: 8400
spring:
  application:
    name: historico-pedidos-service
  jpa:
    hibernate:
      ddl-auto: none
  datasource:
    url: jdbc:mysql://localhost:3306/historicodb
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver
  kafka:
    bootstrap-servers:
      - localhost:9092
eureka:
  client:
    enabled: true
    service-url:
      #defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true